<%= form_with(model: snack, local: true) do |form| %>
  <% if snack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snack.errors.count, "error") %> prohibited this snack from being saved:</h2>

      <ul>
      <% snack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
